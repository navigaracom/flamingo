---
name: idea
description: Use when the user invokes this entry explicitly to turn a vague idea into a structured work item — the full flamingo workflow including format choice. Alias of the main flamingo skill for plugin-namespaced invocation.
---

# Flamingo — idea entry

Read `../flamingo/SKILL.md` (in this plugin's skills directory) and follow it
exactly — the full workflow, including Phase 1's format choice. This entry
adds nothing; it exists so plugin users can type /flamingo:idea.
