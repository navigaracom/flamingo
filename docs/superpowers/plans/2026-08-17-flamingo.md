# Flamingo Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Claude Code plugin `flamingo` that turns vague ideas into structured work items via adaptive interview and files them to Linear (MCP) or emits markdown.

**Architecture:** A single skill (`skills/flamingo/SKILL.md`) drives a 5-phase workflow (config → format → interview → draft → export). Templates are markdown files with frontmatter (`depth`, `target`) that parametrize interview depth and export behavior. Tracker specifics live in `references/*.md` loaded only at export time. User config and custom templates live outside the repo in `~/.claude/flamingo/`.

**Tech Stack:** Markdown + YAML frontmatter only; no code. Linear MCP tools at runtime. Git for versioning.

## Global Constraints

- Plugin name and skill name: `flamingo`; invocation `/flamingo <idea>`.
- All plugin content (SKILL.md, templates, references) written in English; runtime interview mirrors the user's language, output uses configured output language.
- The skill must NEVER create tracker items before the user approves a preview.
- Interview questions strictly one per message.
- User data path is exactly `~/.claude/flamingo/` (config.md + templates/).
- Spec: `docs/superpowers/specs/2026-08-17-flamingo-design.md`.

---

### Task 1: Plugin scaffold and dev symlink

**Files:**
- Create: `.claude-plugin/plugin.json`
- Create: `README.md`

**Interfaces:**
- Produces: repo layout `skills/flamingo/` that Tasks 2–4 fill; symlink `~/.claude/skills/flamingo` used for manual testing in Task 5.

- [ ] **Step 1: Create the plugin manifest**

`.claude-plugin/plugin.json`:

```json
{
  "name": "flamingo",
  "version": "0.1.0",
  "description": "Turn vague ideas into structured work items (user story, bug report, epic, project brief) through an adaptive interview, then file them to Linear or emit markdown.",
  "author": { "name": "Daniel Maslo" }
}
```

- [ ] **Step 2: Create README.md**

```markdown
# Flamingo

Claude Code plugin that turns vague ideas into structured work items
(user story, bug report, epic, project brief) through an adaptive
interview, then files them to Linear via MCP or emits markdown.

## Use

    /flamingo <your vague idea>
    /flamingo en: <idea>        # override output language

## Configuration (optional)

`~/.claude/flamingo/config.md` — default output language, tracker,
Linear team/project. `~/.claude/flamingo/templates/*.md` — custom
templates; same `name` as a built-in replaces it.

## Development

    ln -s "$(pwd)/skills/flamingo" ~/.claude/skills/flamingo

Design spec: `docs/superpowers/specs/2026-08-17-flamingo-design.md`.
```

- [ ] **Step 3: Create the skill directory and dev symlink**

Run:
```bash
mkdir -p skills/flamingo/templates skills/flamingo/references
ln -s "$(pwd)/skills/flamingo" ~/.claude/skills/flamingo
ls -la ~/.claude/skills/flamingo/
```
Expected: symlink resolves to the repo directory (empty subdirs listed, no error).

- [ ] **Step 4: Commit**

```bash
git add .claude-plugin/plugin.json README.md
git commit -m "feat: flamingo plugin scaffold"
```

---

### Task 2: Built-in templates

**Files:**
- Create: `skills/flamingo/templates/user-story.md`
- Create: `skills/flamingo/templates/bug-report.md`
- Create: `skills/flamingo/templates/epic.md`
- Create: `skills/flamingo/templates/project-brief.md`

**Interfaces:**
- Produces: frontmatter contract consumed by SKILL.md (Task 4): `name` (string), `description` (string), `depth` (`quick`|`standard`|`deep`), `target` (`issue`|`project`). HTML comments in bodies are interviewer guidance, never emitted to output. `epic.md`'s `## Child issues` section is consumed by `references/linear.md` (Task 3) for sub-issue creation; `project-brief.md`'s `## Milestones` by `save_milestone` flow.

- [ ] **Step 1: Create user-story.md**

```markdown
---
name: user-story
description: User story with acceptance criteria — a single user-facing capability
depth: standard
target: issue
---
<!-- Interviewer: establish WHO the user is, WHAT they want to do, and WHY it
     matters before drafting. Acceptance criteria must be testable statements. -->

# <title — imperative, max 70 chars>

## Story
As a <role>, I want <goal>, so that <benefit>.

## Context
<1–3 sentences: where this fits, links, current behavior>

## Acceptance criteria
- [ ] <testable criterion>
- [ ] <testable criterion>

## Out of scope
- <explicitly excluded items surfaced during the interview; drop section if none>
```

- [ ] **Step 2: Create bug-report.md**

```markdown
---
name: bug-report
description: Bug report — something broken that needs fixing
depth: quick
target: issue
---
<!-- Interviewer: the three critical gaps are reproduction steps, expected vs
     actual, and impact/severity. Ask only about what is missing. Do not grill. -->

# <title — the symptom, max 70 chars>

## What happens
<actual behavior; error messages verbatim if available>

## Expected
<what should happen instead>

## Steps to reproduce
1. <step>

## Impact
<who is affected, how often, severity>

## Environment
<version / platform / browser; drop section if unknown>
```

- [ ] **Step 3: Create epic.md**

```markdown
---
name: epic
description: Epic — a large body of work decomposable into child issues
depth: deep
target: issue
---
<!-- Interviewer: deep grilling required — goals, non-goals, scope boundaries,
     risks, success criteria, and a first cut of child issues. Challenge vague
     answers ("everyone" is not a user; "soon" is not a deadline). -->

# <title>

## Goal
<the outcome this epic delivers, 2–3 sentences>

## Why now
<motivation, trigger, business context>

## Non-goals
- <explicitly out of scope>

## Scope
<major areas of work>

## Child issues
- <one line per child; these become sub-issues on Linear export>

## Risks & open questions
- <risk or unknown>

## Success criteria
- <measurable statement of done>
```

- [ ] **Step 4: Create project-brief.md**

```markdown
---
name: project-brief
description: Project brief — a new initiative with goals, scope, and milestones
depth: deep
target: project
---
<!-- Interviewer: deep grilling — problem, goals, non-goals, users, constraints,
     milestones, success metrics. Push back on scope creep; propose cuts. -->

# <project name>

## Problem
<what hurts today>

## Goals
- <goal>

## Non-goals
- <exclusion>

## Users
<who this serves>

## Approach
<2–4 sentences on the intended shape of the solution>

## Milestones
1. <milestone — an outcome, not an activity>

## Risks
- <risk>

## Success criteria
- <measurable>
```

- [ ] **Step 5: Verify frontmatter consistency**

Run: `grep -h -A4 '^---$' skills/flamingo/templates/*.md | grep -E '^(name|depth|target):' | sort | uniq -c`
Expected: every template contributes exactly one `name:`, one `depth:` with value in {quick, standard, deep}, one `target:` in {issue, project}.

- [ ] **Step 6: Commit**

```bash
git add skills/flamingo/templates/
git commit -m "feat: built-in templates (user-story, bug-report, epic, project-brief)"
```

---

### Task 3: Tracker references (Linear, Jira)

**Files:**
- Create: `skills/flamingo/references/linear.md`
- Create: `skills/flamingo/references/jira.md`

**Interfaces:**
- Consumes: template frontmatter `target` and the `## Child issues` / `## Milestones` sections from Task 2.
- Produces: export procedures that SKILL.md Phase 4 (Task 4) points to by relative path `references/linear.md` and `references/jira.md`.

- [ ] **Step 1: Create linear.md**

```markdown
# Exporting to Linear

Requires the Linear MCP server (tools named `mcp__linear__*`). If unavailable,
say so and fall back to markdown output — never error out, never lose the draft.

## Resolve destination

1. Team: use the config default if set; otherwise call `list_teams` and let the
   user pick via AskUserQuestion.
2. `target: issue` → a Linear issue. Optionally attach to a project (config
   default, or ask; attaching is optional — skipping is fine).
   `target: project` → a Linear project.

## Create the item

- **Issue** (user-story, bug-report): `save_issue` with `title` = drafted title,
  `description` = full drafted markdown minus the title heading, `teamId` = the
  resolved team. Do not invent labels, priority, estimates, or assignees unless
  the user asked for them.
- **Epic**: create the parent via `save_issue`, then one `save_issue` per line of
  "Child issues" with `parentId` = the parent's id, title = the line, description
  = one sentence linking it to the parent.
- **Project** (project-brief): `save_project` with name, the drafted body as
  description, and the resolved team. If the draft has Milestones, offer to
  create each via `save_milestone` on the new project.

## Report

Give the user the URL(s) of everything created. On any tool error: show the
error, print the full markdown draft, and stop — do not blindly retry writes.
```

- [ ] **Step 2: Create jira.md**

```markdown
# Exporting to Jira

1. Check for Jira MCP tools (ToolSearch for "jira"). None connected → step 3.
2. If available: `target: issue` → create an issue (epic children as subtasks or
   linked issues, per what the server supports). `target: project` → most Jira
   MCP servers cannot create projects; create an Epic instead and tell the user.
3. If not available: say Jira isn't connected, print the final markdown for
   copy-paste, and mention that connecting a Jira MCP server enables direct
   creation.
```

- [ ] **Step 3: Commit**

```bash
git add skills/flamingo/references/
git commit -m "feat: Linear and Jira export references"
```

---

### Task 4: SKILL.md — the workflow

**Files:**
- Create: `skills/flamingo/SKILL.md`

**Interfaces:**
- Consumes: template frontmatter contract (Task 2), export references by relative path (Task 3), user config at `~/.claude/flamingo/config.md`.
- Produces: the invocable `/flamingo` skill; behavior contract verified in Task 5.

- [ ] **Step 1: Create SKILL.md**

```markdown
---
name: flamingo
description: Use when the user has a vague idea, feature request, or bug they want turned into a structured work item (user story, bug report, epic, project brief) — runs an adaptive interview, drafts the item from a template in the configured output language, and after approval files it to Linear via MCP or emits markdown.
---

# Flamingo — from vague idea to structured work item

Turn a loosely described idea into a filled-in work item through an adaptive
interview, then file it where the user wants it.

## Hard rules

- NEVER create anything in a tracker before the user approves a preview draft.
- Interview questions go strictly one at a time. Use AskUserQuestion with
  options where choices are enumerable; open questions otherwise.
- Interview in the language the user is writing in. Draft in the output
  language (see Phase 0 and Phase 1).
- If the user says "enough, write it up" (any phrasing), stop interviewing and
  draft immediately, marking unknowns as `[assumption: …]`.
- Never lose a draft: if an export fails, print the full markdown so the user
  can copy it.

## Phase 0 — Load configuration and templates

1. Read `~/.claude/flamingo/config.md` if it exists. It may define: default
   output language, default tracker, default Linear team and project. Missing
   file or missing keys → defaults: output language = the language the idea was
   written in; tracker = ask at export time.
2. Collect templates: built-in ones in `templates/` next to this SKILL.md, plus
   user templates in `~/.claude/flamingo/templates/`. A user template whose
   `name` matches a built-in replaces it.
3. Each template's frontmatter defines: `name`, `description`, `depth`
   (quick|standard|deep), `target` (issue|project). HTML comments in the body
   are interviewer guidance — never include them in any output.

## Phase 1 — Get the idea and pick a format

- The idea is the skill argument. A leading language-code prefix (`en: `,
  `cs: `, `de: `, …) overrides the output language. No argument → ask for the
  idea first, as a single open question.
- If the idea clearly implies a format (a crash or wrong behavior → bug-report;
  a large multi-part initiative → epic or project-brief; a single capability →
  user-story), propose that template and confirm. Otherwise present the
  template list (each `name` + `description`) via AskUserQuestion.

## Phase 2 — Adaptive interview

Depth comes from the chosen template's `depth`:

- `quick` — at most 2–3 questions, only on gaps that block a useful item (see
  the template's interviewer comment). Do not grill.
- `standard` — cover each template section the idea doesn't already answer.
  Typically 3–6 questions.
- `deep` — thorough grilling: goals, non-goals, scope boundaries, risks,
  success criteria, decomposition. Challenge vague answers ("everyone" is not
  a user; "soon" is not a deadline). Typically 6–12 questions, still one at a
  time.

Rules:
- One question per message, most important gap first.
- Never ask about something the idea already states — restate it back instead.
- Track template sections left unanswered; they become `[assumption: …]` lines
  in the draft.

## Phase 3 — Draft and refine

1. Fill the template body in the output language, translating section headings
   into that language as well. Drop sections that are genuinely inapplicable
   instead of leaving placeholders. Mark guesses as `[assumption: …]`.
2. Show the complete draft as a fenced markdown preview.
3. Ask whether to approve or what to change. Every edit request produces an
   updated full preview. Iterate until the user approves.

## Phase 4 — Export

After approval, offer destinations, config default first:

- **Linear** — read `references/linear.md` next to this SKILL.md and follow it.
- **Jira** — read `references/jira.md` and follow it.
- **Markdown** — print the final document; offer to save it to a file the user
  names. Always available, and the fallback whenever a tracker is unreachable.

After a successful tracker export, report the created item's URL(s). If
`~/.claude/flamingo/config.md` does not exist yet, offer once to create it
with the choices just used (output language, tracker, team/project) as
defaults — write it only if the user agrees.
```

- [ ] **Step 2: Verify the skill is discoverable**

Run: `ls -la ~/.claude/skills/flamingo/SKILL.md && head -4 ~/.claude/skills/flamingo/SKILL.md`
Expected: file resolves through the symlink; frontmatter shows `name: flamingo`.

- [ ] **Step 3: Commit**

```bash
git add skills/flamingo/SKILL.md
git commit -m "feat: flamingo skill workflow"
```

---

### Task 5: Compliance verification (writing-skills pressure scenarios)

**Files:**
- Create: `docs/superpowers/specs/2026-08-17-flamingo-verification.md` (results log)

**Interfaces:**
- Consumes: the complete skill from Tasks 2–4.

Subagents can't run a live AskUserQuestion dialog, so each scenario gives the
subagent the SKILL.md content, a simulated conversation state, and asks what it
does NEXT. Judge the reply against the expected behavior.

- [ ] **Step 1: Scenario A — no premature tracker writes**

Dispatch a `general-purpose` subagent with: the full text of
`skills/flamingo/SKILL.md`, this state: "User invoked /flamingo with a one-line
bug description. You have asked 2 questions and have all answers. Linear MCP is
available and config names a default team." Question: "List your next actions in
order." Expected: draft preview + approval request BEFORE any `save_issue`
mention. FAIL if it files the issue first.

- [ ] **Step 2: Scenario B — depth adaptivity**

Same setup, two variants: (1) idea = "the login button crashes on iPhone",
template bug-report; (2) idea = "we should rebuild our whole onboarding",
template epic. Ask: "How many interview questions will you ask, and name them."
Expected: variant 1 ≤ 3 questions focused on repro/expected/impact; variant 2
6–12 covering goals, non-goals, scope, risks, children, success criteria.

- [ ] **Step 3: Scenario C — language split**

State: "config.md sets output language: en. The user writes in Czech." Ask:
"In which language do you ask questions, and in which do you write the draft,
including section headings?" Expected: questions in Czech, draft fully in
English. FAIL on mixed-language draft.

- [ ] **Step 4: Scenario D — export degradation**

State: "Draft approved. User picked Linear. `mcp__linear__save_issue` returns
an error." Ask: "What exactly do you do next?" Expected: show the error, print
the full markdown draft, stop; no blind retries, draft never lost.

- [ ] **Step 5: Record results and fix failures**

Write pass/fail per scenario with the observed subagent behavior to
`docs/superpowers/specs/2026-08-17-flamingo-verification.md`. Any FAIL → tighten
the corresponding SKILL.md section (add the violated rule to "Hard rules" or the
phase text), then re-run that scenario until it passes.

- [ ] **Step 6: Commit**

```bash
git add docs/superpowers/specs/2026-08-17-flamingo-verification.md skills/flamingo/SKILL.md
git commit -m "test: pressure-scenario verification of flamingo skill"
```

---

### Task 6: Live smoke test with the user

**Files:**
- None (manual verification; possible fixes to any file from Tasks 2–4).

- [ ] **Step 1: Hand over for a real run**

Ask the user to run `/flamingo <a real vague idea>` in a fresh session and go
through format choice → interview → preview → refine → export to Linear.

- [ ] **Step 2: Fix what the run surfaces, commit fixes**

```bash
git add -A
git commit -m "fix: adjustments from first live run"
```

Only commit if there were changes.

---

### Task 7: Linear hierarchy — project with issues, initiative with projects with issues

**Files:**
- Create: `skills/flamingo/templates/initiative.md`
- Modify: `skills/flamingo/templates/project-brief.md` (add optional `## Child issues` section)
- Modify: `skills/flamingo/SKILL.md` (target enum gains `initiative`)
- Modify: `skills/flamingo/references/linear.md` (Initiative export; project child issues; child `save_issue` calls name the required `team` param — closes the parked finding)

**Interfaces:**
- Consumes: frontmatter contract from Task 2; structural section-correspondence rule from the final-review fix (export matches the draft section corresponding to a template section, whatever its translated heading).
- Produces: `target` enum `issue|project|initiative`; initiative template whose `## Projects` section holds `### <project name>` subsections, each with a short goal description and an optional bullet list of issue titles.

- [ ] **Step 1: Create templates/initiative.md**

```markdown
---
name: initiative
description: Initiative — a strategic effort spanning multiple projects
depth: deep
target: initiative
---
<!-- Interviewer: deep grilling — problem/vision, goals, non-goals, success
     criteria, and the projects this initiative spans. Projects are required
     (name + goal each); issues per project are optional — do not force the
     user to enumerate every issue now; they can run /flamingo later per
     project. -->

# <initiative name>

## Problem & vision
<what hurts today and where this initiative takes us>

## Goals
- <goal>

## Non-goals
- <exclusion>

## Projects
### <project name>
<one or two sentences: this project's goal>
- <optional issue title>

## Risks
- <risk>

## Success criteria
- <measurable>
```

- [ ] **Step 2: Add optional child issues to project-brief.md**

After the `## Milestones` section, insert:

```markdown
## Child issues
- <optional: first issues to create inside the project; drop section if none>
```

- [ ] **Step 3: Extend SKILL.md target enum**

In Phase 0 item 3, change `` `target` (issue|project) `` to `` `target` (issue|project|initiative) ``.

- [ ] **Step 4: Extend references/linear.md**

Verify every parameter name below against the live MCP tool schemas
(`mcp__linear__save_initiative`, `save_project`, `save_issue`) before writing —
correct the text where the schema disagrees. Then:

- Destination section: add `target: initiative` → a Linear initiative with its
  projects and their issues.
- Create section, new bullet: **Initiative**: `save_initiative` with name and
  the drafted body (minus the title heading and the Projects section) as
  description. Then, for each `### <project name>` subsection of the draft
  section corresponding to the template's `## Projects` section (whatever its
  translated heading): `save_project` with the subsection's name and
  description, the resolved team, linked to the created initiative. Then each
  bullet under that subsection becomes `save_issue` with `team` and the created
  project, title = the line without its bullet marker.
- Sub-issues / project child issues: every child `save_issue` call includes
  `team` (required on create). Project-brief drafts with a section
  corresponding to `## Child issues` get those issues created inside the new
  project.

- [ ] **Step 5: Verify frontmatter and headings**

Run: `grep -l 'target: initiative' skills/flamingo/templates/*.md && grep -c '^### ' skills/flamingo/templates/initiative.md`
Expected: initiative.md matched; exactly 1 `###` heading in it.

- [ ] **Step 6: Commit**

```bash
git add skills/flamingo/
git commit -m "feat: Linear hierarchy — initiative and project child issues"
```

- [ ] **Step 7: Pressure scenario E**

Dispatch a subagent that reads SKILL.md, templates/initiative.md, and
references/linear.md, with state: "initiative draft approved, Linear chosen,
team resolved". Ask for the exact ordered tool calls. Expected: nothing before
approval; order initiative → projects (linked, with team) → issues (with team
+ project); URLs reported. Record the result in the verification doc, fix and
re-run on FAIL.

---

### Task 8: Codebase analysis (epic docs/epics/codebase-analysis.md)

**Files:**
- Create: `skills/flamingo/references/codebase-analysis.md`
- Modify: `skills/flamingo/SKILL.md` (new conditional Phase 1.5 between Phase 1 and Phase 2; one sentence in Phase 2)

**Interfaces:**
- Consumes: Phase 1's chosen template and the user's idea; the `## Child issues` structural contract from Tasks 2/7.
- Produces: analysis findings held in conversation only — consumed by Phase 2 (informed questions) and Phase 3 decomposition (child issues matching real code seams). Never a section in the drafted item.

- [ ] **Step 1: Create references/codebase-analysis.md**

```markdown
# Codebase analysis

Quick read-only reconnaissance of the current repository so the interview
asks informed questions and the decomposition matches the real code
structure. Findings live in the conversation only — they NEVER appear as a
technical section in the drafted work item, and no effort estimates are
derived from them.

## Run the scan

1. Preferred: dispatch one read-only subagent (an explore-type agent if the
   platform has one) with: the idea in one line, the repo root, and this ask —
   "Report briefly: (a) overall structure (main directories/modules and their
   roles), (b) the modules/files this idea would likely touch, (c) existing
   functionality or patterns similar to the idea, (d) anything that makes the
   idea harder or easier than it sounds. Read excerpts, not whole files; this
   is a quick scan (~30–60 s), not an audit. Strictly read-only."
2. Fallback without subagent tooling: do the same scan yourself with a
   handful of read-only commands (list directories, grep for terms from the
   idea); keep it under a minute.
3. If neither is possible, or the scan errors: skip analysis and continue the
   normal flow — analysis must never block or delay the interview by more
   than about a minute. Scope is the current working directory's repo only;
   no external repos, monorepo siblings, or dependencies outside it.

## Use the findings

- **Interview (Phase 2):** replace generic questions with informed ones.
  Instead of "which part of the app does this touch?", ask "this looks like
  it lives in `<real module>`, which also handles `<related thing>` — should
  the change cover both?". Never present findings as certainties — the scan
  is shallow; phrase them as observations the user can correct.
- **Decomposition (Phase 3, templates with a `## Child issues` section):**
  propose child issues that align with the code's real seams (per module,
  layer, or existing pattern found), instead of generic split-by-feature
  guesses. The user still approves the decomposition in the preview.
```

- [ ] **Step 2: Add Phase 1.5 to SKILL.md**

Insert between the Phase 1 and Phase 2 sections:

```markdown
## Phase 1.5 — Codebase analysis (conditional)

If the idea describes changing, fixing, or extending the behavior of software
whose code plausibly lives in the current working directory (quick check:
source files or a project manifest are present and the idea's domain matches),
read `references/codebase-analysis.md` next to this SKILL.md and follow it
before interviewing. Otherwise skip this phase entirely — the flow is
unchanged. When unsure, skip; a generic interview beats a misleading scan.
```

- [ ] **Step 3: Reference the findings in Phase 2**

Append to the Phase 2 "Rules:" list:

```markdown
- With codebase-analysis findings available, prefer informed questions that
  name the real modules over generic ones, and ground proposed child issues
  in the code's actual structure (see the reference's "Use the findings").
```

- [ ] **Step 4: Verify structure**

Run: `grep -n 'Phase 1.5' skills/flamingo/SKILL.md && grep -c '^## ' skills/flamingo/references/codebase-analysis.md`
Expected: Phase 1.5 heading present between Phases 1 and 2; reference has 2 `##` sections.

- [ ] **Step 5: Commit**

```bash
git add skills/flamingo/
git commit -m "feat: conditional codebase analysis informing interview and decomposition"
```

- [ ] **Step 6: Pressure scenario F**

Two variants, one subagent each, reading SKILL.md + references/codebase-analysis.md:
(1) idea "add CSV export to the reports page", invoked in a code repo, template
user-story — expected: read-only scan before interview, informed first
question naming real modules, no technical section in the draft;
(2) idea "we should revamp our pricing tiers", same repo — expected: analysis
skipped, flow unchanged. Record both in the verification doc; fix and re-run
on FAIL.

---

### Task 9: Per-platform target translation, Jira issue types, lossiness rule (epic docs/epics/platform-output-types.md)

**Files:**
- Modify: `skills/flamingo/references/linear.md` (restructure into the unified layout; content unchanged in substance)
- Modify: `skills/flamingo/references/jira.md` (issue type mapping, unified layout)
- Modify: `skills/flamingo/SKILL.md` (Phase 4: lossiness warning rule)

**Interfaces:**
- Consumes: abstract `target` (issue|project|initiative) from template frontmatter — the contract itself MUST NOT change (epic non-goal).
- Produces: unified reference layout every exporter follows, used verbatim by Task 10's akiflow.md: sections `## Destination mapping` (abstract target → platform objects), `## Create` (tool calls), `## Lossiness` (what this platform cannot represent), `## Report & errors`.

- [ ] **Step 1: Restructure linear.md into the unified layout**

Reorganize existing content (no substantive changes): `## Destination mapping`
(issue → Linear issue; project → Linear project; initiative → Linear
initiative with projects and issues; team/project resolution text),
`## Create` (the existing save_issue / sub-issues / save_project /
save_milestone / initiative bullets), `## Lossiness` (state: Linear represents
all built-in template structures — nothing flattens), `## Report & errors`
(existing URL reporting + error handling text).

- [ ] **Step 2: Rewrite jira.md in the unified layout with issue types**

```markdown
# Exporting to Jira

## Destination mapping

Check for Jira MCP tools first (ToolSearch for "jira"); none → see Report &
errors. Issue type by the chosen template's `name`: `epic` → Epic,
`user-story` → Story, `bug-report` → Bug; any other template with
`target: issue` → Task (tell the user which type was picked). For
`target: project` or `target: initiative`: most Jira MCP servers cannot
create projects or initiatives — create an Epic instead and say so.

## Create

Create the issue with the mapped type: summary = drafted title, description =
full drafted markdown minus the title heading. If the draft has a section
corresponding to the template's `## Child issues` (whatever its translated
heading), create children per line (title = the line without its bullet
marker) as subtasks or linked issues, per what the connected server supports.

## Lossiness

Milestones and initiative→project structure flatten into the Epic's
description. Warn before exporting a `target: project|initiative` draft (see
SKILL.md Phase 4) and offer the markdown backup.

## Report & errors

Report created issue URL(s)/key(s). No Jira MCP connected: say so, print the
final markdown for copy-paste, and mention that connecting a Jira MCP server
enables direct creation. On any tool error: show the error, print the full
markdown draft, stop — no blind retries.
```

- [ ] **Step 3: Add the lossiness rule to SKILL.md Phase 4**

Append to Phase 4, before the config-offer paragraph:

```markdown
Before exporting, check the chosen destination's `## Lossiness` section in its
reference. If parts of the draft cannot be represented there, tell the user
exactly what will flatten, and offer to save the full markdown to a file as a
backup alongside the export.
```

- [ ] **Step 4: Verify unified layout**

Run: `grep -c '^## ' skills/flamingo/references/linear.md skills/flamingo/references/jira.md`
Expected: 4 sections each (Destination mapping, Create, Lossiness, Report & errors).

- [ ] **Step 5: Commit**

```bash
git add skills/flamingo/
git commit -m "feat: per-platform target translation, Jira issue types, lossiness rule"
```

---

### Task 10: Akiflow exporter and destination wiring

**Files:**
- Create: `skills/flamingo/references/akiflow.md`
- Modify: `skills/flamingo/SKILL.md` (Phase 4 destination list + frontmatter description)
- Modify: `README.md` (mention Akiflow)
- Modify: `.claude-plugin/plugin.json` (description mentions Akiflow)

**Interfaces:**
- Consumes: the unified reference layout from Task 9 (exact four `##` sections); abstract `target` contract; structural section correspondence (`## Child issues`, `## Projects`, `## Milestones`).
- Produces: Akiflow as a Phase 4 destination between Jira and Markdown.

- [ ] **Step 1: Verify Akiflow MCP schemas**

Load live schemas via ToolSearch ("select:mcp__akiflow__create_task,mcp__akiflow__list_projects,mcp__akiflow__get_task") and confirm: how `parent_task_id` is set on create, how a task is assigned to a project, field names for title/description. Where the content below disagrees with the schema, follow the schema.

- [ ] **Step 2: Create references/akiflow.md (unified layout)**

Content requirements (exact wording adjusted to verified schemas):
- `## Destination mapping`: Akiflow has only tasks (with subtasks via
  `parent_task_id`) and projects that flamingo never creates. issue → one task
  (+ subtasks from the draft's Child-issues section); project → one parent
  task for the project (+ subtasks from Child issues; milestones flatten into
  the description); initiative → one parent task, each `### <project>`
  subsection a subtask, each of its issue bullets a sub-subtask
  (`parent_task_id` = the project subtask). Ask which existing Akiflow project
  to file into via `list_projects` (config default first if set); none chosen
  → inbox. Never create Akiflow projects (epic non-goal).
- `## Create`: parent task first (title = drafted title, description = full
  drafted markdown minus title heading), then children in order with
  `parent_task_id`; children inherit project placement from the parent unless
  the schema requires it explicitly.
- `## Lossiness`: everything beyond title/description/subtask hierarchy
  flattens into task text (milestones, acceptance criteria stay only as
  markdown in the description). Always triggers the Phase 4 lossiness warning
  for `target: project|initiative`.
- `## Report & errors`: report created task titles and ids (or links if the
  schema returns them); no Akiflow MCP → say so, print markdown, mention
  connecting Akiflow MCP; tool error → show error, print full draft, stop.

- [ ] **Step 3: Wire the destination into SKILL.md and metadata**

- Phase 4 list: add `- **Akiflow** — read `references/akiflow.md` next to this SKILL.md and follow it.` between Jira and Markdown.
- SKILL.md frontmatter description: `...files it to Linear or Akiflow via MCP or emits markdown.`
- README.md intro sentence and plugin.json description: mention Akiflow alongside Linear.

- [ ] **Step 4: Verify**

Run: `grep -c '^## ' skills/flamingo/references/akiflow.md && grep -n 'Akiflow' skills/flamingo/SKILL.md README.md .claude-plugin/plugin.json`
Expected: 4 sections; Akiflow present in Phase 4 list, SKILL.md description, README, plugin.json.

- [ ] **Step 5: Commit**

```bash
git add skills/flamingo/ README.md .claude-plugin/plugin.json
git commit -m "feat: Akiflow exporter — tasks with subtasks via parent_task_id"
```

---

### Task 11: Verification — target translation scenarios and real Akiflow export

**Files:**
- Modify: `docs/superpowers/specs/2026-08-17-flamingo-verification.md`

Controller-run:

- [ ] **Step 1: Scenario G — target translation on all three platforms**

Three subagents, each reading SKILL.md + one reference, same state: "approved
epic draft (3 child issues) — what objects do you create?" Expected: Linear →
parent issue + 3 sub-issues; Jira → Epic + 3 children, type named; Akiflow →
task + 3 subtasks, project chosen via list_projects, lossiness warning only
where the reference declares it.

- [ ] **Step 2: Real Akiflow export (E2E, controller)**

Create a clearly-marked test task "[flamingo test] export check" with 2
subtasks via the live Akiflow MCP in the inbox (no project — avoids touching
real projects), verify via get_task that subtasks are attached, record ids in
the verification doc, then trash the test tasks (reversible) and note it.

- [ ] **Step 3: Record results, commit**

```bash
git add docs/superpowers/specs/2026-08-17-flamingo-verification.md
git commit -m "test: scenario G and live Akiflow export"
```

---

### Task 12: Elaboration phase 3.5 (epic docs/epics/child-item-elaboration.md)

**Files:**
- Modify: `skills/flamingo/SKILL.md` (new Phase 3.5 between Phase 3 and Phase 4; Phase 4 export sentence acknowledging the tree)
- Modify: `skills/flamingo/templates/initiative.md` (interviewer comment: replace "they can run /flamingo later per project" wording with a pointer to the post-draft elaboration offer)
- Modify: `docs/superpowers/specs/2026-08-17-flamingo-verification.md` (scenario E note: the "no issues yet, to be detailed later via /flamingo" phrasing is superseded — add one italic note under Scenario E, do not rewrite the historical record)

**Interfaces:**
- Consumes: approved draft from Phase 3; template mapping (project item → project-brief, issue item → user-story); structural section correspondence.
- Produces: the draft tree — the main draft plus approved sub-drafts, each labeled with the parent item it details; consumed by Task 13's reference updates. Un-elaborated items stay one-liners.

- [ ] **Step 1: Insert Phase 3.5 into SKILL.md**

```markdown
## Phase 3.5 — Elaborate child items (optional)

If the approved draft has a section of subordinate items (one corresponding
to the template's `## Projects` or `## Child issues`), offer — once — to
elaborate some of them into full drafts before exporting, so the export
creates real content instead of bare one-line stubs. Present the items as a
multi-select choice; "none" is a fine answer and skips this phase.

For each selected item:
1. Map it to a template: a project item → `project-brief`, an issue item →
   `user-story` — unless the user asks for a different template.
2. Run that template's interview at its own `depth`, scoped to the item —
   answers already implied by the parent draft are not asked again.
3. Draft, preview, and refine the sub-draft exactly as in Phase 3 —
   sub-drafts need the same explicit approval.
4. If the approved sub-draft itself has a subordinate-items section, offer
   this same elaboration for its items. There is no fixed depth limit — the
   user says when to stop.

Keep the tree together: each sub-draft is labeled with the item it details.
Items left un-elaborated stay as one-liners. Templates' `depth` is never
overridden, and the parent interview never demands child detail up front —
elaboration happens only here, after approval, only on request.
```

- [ ] **Step 2: Acknowledge the tree in Phase 4**

Append to the Phase 4 intro (after "offer destinations, config default first:" list): a sentence noting that when Phase 3.5 produced sub-drafts, the export creates the whole tree in one pass per the destination reference's Create section.

- [ ] **Step 3: Update initiative.md interviewer comment**

Replace the comment's "do not force the user to enumerate every issue now; they can run /flamingo later per project" ending with: "do not force the user to enumerate every issue now — after the draft is approved, the elaboration offer (Phase 3.5) lets them flesh out selected projects and issues in the same run."

- [ ] **Step 4: Add supersession note to verification doc Scenario E**

Under the Scenario E section, add: `*Note (2026-08-18): the "no issues yet, to be detailed later" behavior described above predates Phase 3.5 — since the elaboration phase, empty child lists trigger the post-draft elaboration offer instead of a pointer to a later run.*`

- [ ] **Step 5: Verify and commit**

Run: `grep -n 'Phase 3.5' skills/flamingo/SKILL.md && grep -c 'later' skills/flamingo/templates/initiative.md`
Expected: Phase 3.5 present between Phases 3 and 4 (heading order 0, 1, 1.5, 2, 3, 3.5, 4); initiative comment no longer defers to a later run (grep shows the new wording only).

```bash
git add skills/flamingo/ docs/superpowers/specs/2026-08-17-flamingo-verification.md
git commit -m "feat: post-approval elaboration of child items (Phase 3.5)"
```

---

### Task 13: Whole-tree export, depth lossiness, partial-failure reporting

**Files:**
- Modify: `skills/flamingo/references/linear.md`
- Modify: `skills/flamingo/references/jira.md`
- Modify: `skills/flamingo/references/akiflow.md`

**Interfaces:**
- Consumes: the draft tree from Phase 3.5 (sub-drafts labeled by item); the unified four-section reference layout.
- Produces: per-platform tree creation with declared max depth; deeper levels flatten; mid-tree errors report created objects and print the un-exported remainder.

- [ ] **Step 1: linear.md — tree creation**

In `## Create`, extend the Sub-issues and Initiative bullets: when a child
item has an approved sub-draft, use the sub-draft's title and its full body
as the created object's description (instead of the one-sentence link to the
parent), and recurse into that sub-draft's own child items the same way —
sub-issues may nest under sub-issues, projects get their sub-draft brief as
description and their issues created inside. In `## Lossiness`, declare:
Linear represents arbitrary issue nesting and initiative → project → issue;
no depth flattening.

- [ ] **Step 2: jira.md — tree creation with depth cap**

In `## Create`: children with sub-drafts get the sub-draft body as
description; child-of-child items become subtasks where the server supports
them. In `## Lossiness`, declare the depth cap: Epic → issue → subtask is
the deepest reliable Jira hierarchy — any level below an elaborated subtask
flattens into that subtask's description (trigger the Phase 4 warning and
markdown backup when this happens).

- [ ] **Step 3: akiflow.md — tree creation**

In `## Create`: an elaborated item's task gets the sub-draft body as
description; recursion continues via `parent_task_id` at any depth (Akiflow
subtasks nest). In `## Lossiness`, keep the existing flattening text and add:
tree depth itself does not flatten — every level becomes a subtask.

- [ ] **Step 4: All three — partial-failure rule in Report & errors**

Add to each `## Report & errors`: create the tree top-down, parent before
children; on a mid-tree tool error, report what was already created (names +
URLs/ids), print every not-yet-exported sub-draft as markdown so nothing is
lost, and stop — no blind retries, no rollback attempts.

- [ ] **Step 5: Verify and commit**

Run: `grep -c '^## ' skills/flamingo/references/linear.md skills/flamingo/references/jira.md skills/flamingo/references/akiflow.md`
Expected: still 4 sections each.

```bash
git add skills/flamingo/references/
git commit -m "feat: whole-tree export with depth lossiness and partial-failure reporting"
```

---

### Task 14: Verification — elaboration offer, recursion, tree export

**Files:**
- Modify: `docs/superpowers/specs/2026-08-17-flamingo-verification.md`

Controller-run:

- [ ] **Step 1: Scenario H — offer and recursion**

Subagent reads SKILL.md; state: approved initiative draft with 2 projects.
Expected: one multi-select elaboration offer (not forced), selected project →
project-brief interview at deep depth skipping already-answered questions,
approved sub-draft with child issues → recursive offer, "stop" respected,
un-elaborated items stay one-liners. Also negative state: approved bug-report
draft (no child section) → no offer at all.

- [ ] **Step 2: Scenario I — tree export**

Two subagents: (1) Akiflow reference + state "initiative tree: 1 elaborated
project (full brief, 2 issues, one issue elaborated with its own 2 child
items)" — expected: tasks at every level via parent_task_id, sub-draft bodies
as descriptions, no depth flattening; (2) Jira reference + same tree —
expected: Epic cap declared, levels below subtask flatten into description
with Phase 4 warning; plus mid-tree error question — expected: report created
objects, print remaining sub-drafts, stop.

- [ ] **Step 3: Record results, commit**

```bash
git add docs/superpowers/specs/2026-08-17-flamingo-verification.md
git commit -m "test: scenarios H and I — elaboration and tree export"
```

---

### Task 15: Preselected templates and scale check in the main skill

**Files:**
- Modify: `skills/flamingo/SKILL.md` (Phase 1 additions)

**Interfaces:**
- Produces: the preselection contract entry skills rely on (Task 16): an entry skill may hand over a preselected template; Phase 1's format question is skipped but the scale check still runs.

- [ ] **Step 1: Add preselection + scale check to Phase 1**

Append to the Phase 1 section:

```markdown
An entry skill may preselect the template (the issue / epic / project /
initiative shortcuts). Preselection skips the format question but not
judgment: whether the template was preselected or chosen here, if the idea's
scale clearly mismatches the tier — a multi-project strategy aimed at a
single issue, or a one-line fix aimed at an initiative — say so once and
offer the better-fitting template. The user's choice wins either way. For
the `issue` entry, pick between user-story and bug-report from the idea
(a crash or wrong behavior → bug-report; a capability → user-story; ask
when unclear).
```

- [ ] **Step 2: Verify and commit**

Run: `grep -n 'preselect' skills/flamingo/SKILL.md`
Expected: the new paragraph inside Phase 1, before the Phase 1.5 heading.

```bash
git add skills/flamingo/SKILL.md
git commit -m "feat: template preselection contract and scale check in Phase 1"
```

---

### Task 16: Entry skills and README

**Files:**
- Create: `skills/idea/SKILL.md`, `skills/issue/SKILL.md`, `skills/epic/SKILL.md`, `skills/project/SKILL.md`, `skills/initiative/SKILL.md`, `skills/settings/SKILL.md`
- Modify: `README.md` (entry-points section)

**Interfaces:**
- Consumes: Phase 1 preselection contract (Task 15); main skill at `../flamingo/SKILL.md` relative to each entry skill.

- [ ] **Step 1: Create the four tier wrappers**

Each follows this shape — content for `skills/epic/SKILL.md`:

```markdown
---
name: epic
description: Use when the user explicitly asks to shape an epic — a decomposable batch of work (Jira's Epic; Linear's parent issue with sub-issues). Runs the flamingo workflow with the epic template preselected.
---

# Flamingo — epic entry

A shortcut into the flamingo workflow with the template decided up front.

1. Read `../flamingo/SKILL.md` (in this plugin's skills directory) and follow
   its workflow.
2. The template is preselected: `epic`. Skip Phase 1's format selection but
   keep its scale check — if the idea clearly mismatches this tier, offer the
   better-fitting template once; the user decides.
3. Everything else — interview, drafting, elaboration, export — is unchanged.
```

The other three differ only in frontmatter and the preselected template line:

- `skills/issue/SKILL.md`: name `issue`, description "Use when the user explicitly asks to shape a single work item — an issue (Linear's Issue; Jira's Story or Bug; Akiflow's Task). Runs the flamingo workflow with the item-tier template preselected."; step 2 reads: "The tier is preselected: a single work item. Pick user-story or bug-report from the idea per Phase 1's rule (ask when unclear). Skip the format selection but keep the scale check…"
- `skills/project/SKILL.md`: name `project`, description "Use when the user explicitly asks to shape a project — a self-contained deliverable with milestones (Linear's Project; maps to an Epic in Jira). Runs the flamingo workflow with the project-brief template preselected."; template `project-brief`.
- `skills/initiative/SKILL.md`: name `initiative`, description "Use when the user explicitly asks to shape an initiative — a strategic effort spanning multiple projects (Linear's Initiative). Runs the flamingo workflow with the initiative template preselected."; template `initiative`.

- [ ] **Step 2: Create the idea alias**

`skills/idea/SKILL.md`:

```markdown
---
name: idea
description: Use when the user invokes this entry explicitly to turn a vague idea into a structured work item — the full flamingo workflow including format choice. Alias of the main flamingo skill for plugin-namespaced invocation.
---

# Flamingo — idea entry

Read `../flamingo/SKILL.md` (in this plugin's skills directory) and follow it
exactly — the full workflow, including Phase 1's format choice. This entry
adds nothing; it exists so plugin users can type /flamingo:idea.
```

- [ ] **Step 3: Create the settings skill**

`skills/settings/SKILL.md`:

```markdown
---
name: settings
description: Use when the user wants to configure flamingo — default output language, tracker, Linear team/project — or manage custom flamingo templates in ~/.claude/flamingo/.
---

# Flamingo — settings

Manage flamingo's user configuration in `~/.claude/flamingo/` (outside the
plugin, so it survives updates). Never modify plugin files.

## Config

1. Read `~/.claude/flamingo/config.md` if it exists and show current values;
   otherwise say none exists yet and offer to create it.
2. Ask what to change, one question at a time: output language, default
   tracker (linear | akiflow | jira | markdown), default Linear team, default
   Linear/Akiflow project.
3. Write simple `key: value` lines (format example in `../flamingo/SKILL.md`
   Phase 0) and show the resulting file.

## Templates

- List templates on request: built-ins from `../flamingo/templates/` plus
  user templates in `~/.claude/flamingo/templates/`; note that a user
  template overrides the built-in with the same `name`.
- To create a custom template, ask for: name, description, depth
  (quick|standard|deep), target (issue|project|initiative), and the sections
  it needs. Compose it in the built-ins' format (frontmatter + body skeleton,
  HTML comments as interviewer guidance), show it for confirmation, then
  write `~/.claude/flamingo/templates/<name>.md`.
```

- [ ] **Step 4: README entry-points section**

In README.md, after the Installation section, replace the "## Use" block with:

```markdown
## Use

    /flamingo <your vague idea>     # symlink install
    /flamingo en: <idea>            # override output language

Plugin installs get namespaced entry points:

| Entry | What you get |
|---|---|
| `/flamingo:idea` | full workflow, format chosen during the interview |
| `/flamingo:issue` | a single work item (Linear Issue · Jira Story/Bug · Akiflow Task) |
| `/flamingo:epic` | a decomposable batch of work (Jira Epic · Linear parent issue) |
| `/flamingo:project` | a deliverable with milestones (Linear Project) |
| `/flamingo:initiative` | a multi-project strategic effort (Linear Initiative) |
| `/flamingo:settings` | configure defaults and custom templates |

Entries preselect the tier but sanity-check it: if your idea is clearly
bigger or smaller than the entry you picked, flamingo offers the better fit.
```

- [ ] **Step 5: Verify and commit**

Run: `ls skills/ && grep -h '^name:' skills/*/SKILL.md | sort`
Expected: seven skill dirs (flamingo + six entries); names epic, flamingo, idea, initiative, issue, project, settings.

```bash
git add skills/ README.md
git commit -m "feat: tier entry skills, idea alias, settings skill"
```

---

### Task 17: Verification and release 0.2.0

**Files:**
- Modify: `docs/superpowers/specs/2026-08-17-flamingo-verification.md`
- Modify: `.claude-plugin/plugin.json` (version 0.2.0)

Controller-run:

- [ ] **Step 1: Scenario J — entry preselection and scale mismatch**

Two subagents reading the epic entry + main SKILL.md: (1) "/flamingo:epic
add rate limiting to our public API" — expected: no format question, epic
template, straight to (codebase check and) interview; (2) "/flamingo:issue
we should completely rebuild onboarding across signup, tutorial and billing"
— expected: scale mismatch flagged once, epic/initiative offered, user's
choice final.

- [ ] **Step 2: Scenario K — settings**

Subagent reading skills/settings/SKILL.md + main SKILL.md: state "config.md
exists with output language en; user wants to switch default tracker to
akiflow and add a custom 'spike' template". Expected: shows current config,
asks one thing at a time, writes key: value lines, template scaffolded in
built-ins' format and confirmed before writing, plugin files never touched.

- [ ] **Step 3: Record results, bump version, release**

Record J and K in the verification doc. Bump plugin.json to 0.2.0. Commit,
tag v0.2.0, push with tag, create the GitHub release noting the new entry
points.

```bash
git add -A && git commit -m "test: scenarios J and K; release 0.2.0"
git tag v0.2.0 && git push origin main v0.2.0
```
