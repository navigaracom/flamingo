---
name: project
description: Use when the user explicitly asks to shape a project — a self-contained deliverable with milestones (Linear's Project; maps to an Epic in Jira). Runs the flamingo workflow with the project-brief template preselected.
---

# Flamingo — project entry

A shortcut into the flamingo workflow with the template decided up front.

1. Read `../flamingo/SKILL.md` (in this plugin's skills directory) and follow
   its workflow.
2. The template is preselected: `project-brief`. Skip Phase 1's format
   selection but keep its scale check — if the idea clearly mismatches this
   tier, offer the better-fitting template once; the user decides.
3. Everything else — interview, drafting, elaboration, export — is unchanged.
